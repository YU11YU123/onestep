# OneStep 项目协作规则

## 开工顺序

1. 先读 `README.md`、`PROGRESS.md`、`BLOCKED.md`。
2. 再读本次涉及的 `docs/` 文件，不得用聊天中的印象替代已落盘规格。
3. 每完成一个可验收步骤立即更新 `PROGRESS.md`；不确定但不阻碍其他工作的事项写入 `BLOCKED.md`。

## 项目与命令

- OneStep 原型位于 `onestep-app/`，当前技术栈为 React 19、TypeScript、Vite 8。
- 启动：在 `onestep-app/` 运行 `npm.cmd run dev -- --host 127.0.0.1 --port 4173`。
- 验证：运行 `npm.cmd run typecheck`、`npm.cmd run lint`、`npm.cmd run build`；浏览器回归使用 `node scripts/smoke-ui.mjs`，需先有 9222 端口的调试浏览器。
- `black-white-components/` 只是用户的开发素材库，不是 OneStep 产品模块；默认只读，不在其中开发 OneStep 功能。

## 当前状态

- 可点击原型和第一轮界面迭代已完成，真实数据库、Windows 桌面能力、通知和 DeepSeek 尚未实现。
- 当前仍处于“大白话开发顺序”的第 2 步：继续按用户体验修改界面。只有用户明确确认界面框架后，才进入本地数据阶段。

## 产品边界

- 第一版只做 Windows、本地单用户；不擅自加入账号、云同步、团队协作或数据分析。
- 快捷记录、任务数据和提醒不依赖 AI。DeepSeek 不可用时必须降级，不得阻止用户保存任务。
- “截止时间”“计划日期”“提醒时间”是三个不同字段，不得合并。
- 四象限由“是否重要”和“是否紧急”组合计算：否/是＝紧急不重要，是/否＝重要不紧急，是/是＝紧急且重要，否/否＝不紧急不重要；不另设高/中/低优先级。
- 收集箱任务在尚未判断时允许两个字段为空；“未分类”是整理状态，不等于“不紧急不重要”。任务完成整理后，两个字段必须都有明确的是/否值。
- 图片作为附件保存，数据库只保存元数据和本地路径；删除任务时默认不立即永久删除附件。

## 安全与数据

- API Key 不写入源码、SQLite、日志、截图或错误报告；使用 Windows 安全凭据或等价的受保护存储。
- 调用 AI 前明确展示将上传的内容范围。默认只发送当前任务及用户主动选择的相关记忆。
- 不建立遥测，不上传本地任务库，不将工作内容用于隐式训练素材。
- 数据库结构变更必须使用迁移；迁移前创建可恢复备份，并验证旧数据仍可读取。

## 工程纪律

- 新增依赖前记录用途、替代方案和维护风险；不安装全局依赖、不修改系统配置，除非用户明确同意。
- 不用跳过测试、放宽断言、删除测试、伪造 mock、修改验收阈值或吞掉错误来制造绿灯。
- 功能完成的含义是：自动化检查通过，并在 Windows 上按 `docs/01-产品需求.md` 的对应场景实际走通。
- 保留用户无关改动；不使用 reset、checkout 或覆盖原件的方式处理冲突。
