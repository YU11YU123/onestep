# 开发进度

更新时间：2026-08-08

## 当前阶段

- 阶段 0“需求与基线”：已完成。
- 大白话第 1 步“可点击界面框架”：已完成。
- 大白话第 2 步“按照用户感觉修改界面”：进行中；截至 2026-08-08 的 50 项界面与交互反馈已实现，尚未冻结框架。
- 下一项是第一轮核心动效：弹窗开关、详情展开/收起、任务完成/恢复/移动、选择层和 AI 拆解状态切换。
- “计划日期单行入口”仍在试验：默认仅显示当前安排，点击后再选择今天、明天、具体日期或以后再做；等待用户体验后决定保留或回退。
- 下一阶段不是 Windows 打包，而是大白话第 3 步“本地数据真实保存”。只有用户确认界面后才开始。

## 当前可运行产物

- 应用目录：`D:\only me\onestep-app`。
- 技术栈：React 19、TypeScript、Vite 8。
- 启动命令：在应用目录运行 `npm.cmd run dev -- --host 127.0.0.1 --port 4173`。
- 当前是浏览器可点击原型，使用内存中的演示任务；刷新页面会恢复初始数据。
- `black-white-components/` 只是用户的开发素材库，不属于 OneStep 产品模块；需要的 Button、Input、Textarea、Badge 已复制到应用自己的 `src/components/ui/`，素材库保持只读。

## 版本控制与远程备份

- 2026-08-06 已在工作区根目录建立 Git 仓库，默认分支为 `main`。
- 私有远程仓库：`https://github.com/YU11YU123/onestep`。
- 远程范围仅包含项目文档与 `onestep-app/` 源码、配置和测试脚本。
- `.tools/`、`.obsidian/`、`black-white-components/`、`node_modules/`、`dist/` 和生成的界面截图均不上传。
- 本次实现提交后，本地 `main` 将比本地记录的 `origin/main` 领先 17 个提交；这些界面迭代尚未推送远程。

## 已完成的产品框架

- 左侧：收集箱、今天、未来、以后、已完成、四象限和项目。
- 中间：任务搜索、任务列表、任务完成和选择。
- 右侧：任务标题、第一步、计划日期、截止时间、提醒时间、项目、一次性/每天重复、四象限、备注和附件入口。
- 左下角：头像和账户菜单；省略号菜单只显示设置、剩余用量与版本，快捷键统一在设置页查看和修改。
- 快捷记录弹窗、剪贴板图片预览、安排明天弹窗、AI 第一步模拟拆解与反馈后重新拆解。
- 浏览器页面获得焦点时，`Ctrl+Alt+Space` 可模拟快捷记录；这不是真正的 Windows 全局快捷键。

## 用户反馈完成清单（完整）

1. 四象限含义修正并固定为：紧急不重要、重要不紧急、紧急且重要、不紧急不重要。
2. “已完成”列表可显示完成任务，并提供恢复操作；误点完成后可以回到原列表。
3. 提醒选择先使用自定义月历确定日期，再通过小时、分钟滚轮精确选择并确认。
4. 项目选择不再使用系统原生下拉框，改为与界面一致的黑白自定义选单。
5. 删除左侧重复的“快速记录”，顶部只保留“记下一件事”；键盘模拟入口仍保留。
6. 快捷记录提示从“领导说了什么”改为“你现在在想什么？”。
7. 右侧任务详情可以关闭；点击任一任务会重新展开详情。
8. 项目区加号可新增自定义项目，并阻止空名称和重复名称。
9. 项目名称平时保持普通文字；双击后在原位置变成输入框，并自动全选当前名称。
10. 项目重命名会同步更新已有任务的项目归属和当前项目选择。
11. 重命名可按 Enter、点击最右侧对勾或点击页面任意其他位置保存；按 Esc 放弃；空名称保留原名称。
12. 重命名编辑行保持原项目条完整宽度且不溢出侧边栏；确认按钮位于最右侧，使用黑色圆角方形和白色对勾，内外圆角均为 8px。
13. 右侧详情的任务标题、AI 入口、字段标签、日期/项目控件、四象限、备注、附件和步骤字号已统一放大。
14. 左下角头像、姓名和“本地模式”字号已放大。
15. 快捷记录弹窗主输入从 20px 调整为 17px，提示颜色加深，上方间距缩短。
16. 快捷记录弹窗的图标与“先记下来＋说明”文字块改为垂直居中，标题调为 19px，行距和横向间距已协调。
17. 主内容标题改用明确的中文无衬线字体，日期、标题和说明改为统一 6px 网格间距，不再由不一致的上下边距拼接。
18. 四象限页的搜索栏、任务数量、象限标题、动作标签、任务文字、勾选圆点和空状态均已提高字号与可读性。
19. 快捷记录弹窗加宽到 680px，输入区提高到 132px；底栏文字提高到 12px，“保存到收集箱”按钮提高到 42px、14px 字号。
20. 快捷记录底栏左右两端文字统一为 14px；中间快捷键说明保持 12px，主次仅通过颜色、字重和按钮背景区分。
21. 主内容顶部的日期、标题和说明由 6px 间距收紧为 3px，并略微压缩标题行高。
22. 快捷记录弹窗关闭按钮改为右上角独立定位，距顶部和右侧各 16px，不再跟随标题行居中。
23. 四象限卡片标题与第一条任务之间的上边距由 17px 收紧为 8px，任务点击高度保持不变。
24. 四象限卡片顶部内边距由 18px 增加到 22px；连续任务之间的间距由 6px 缩短为 2px。
25. 普通任务卡取消固定最小高度，改用上下各 18px 的对称内边距；下方时间、项目和分类文字由 9px 提高到 10px。
26. 再次点击当前已选象限会取消选择，清空重要/紧急判断与计划日期，并把任务退回收集箱；项目、提醒、截止信息、备注和附件保持不变。
27. AI 入口与结果内容字号已提高，“开始拆解”按钮字号已降低；结果页新增反馈输入框，可根据用户问题重新生成拆解建议，再决定是否采用第一步。
28. 普通任务卡的回形针附件图标改为紧跟任务标题，缩短图标与文字间距并统一垂直对齐，不再形成视觉断层。
29. AI 入口重新平衡图标、标题、说明和箭头：保留高于初版的可读字号，降低标题字重、收紧行距，并统一为中文界面字体，避免各元素互相争抢注意力。
30. “当前第一步”卡片收紧标题到首项以及相邻步骤之间的垂直距离，步骤圆点与文字顶部重新对齐。
31. 右侧详情的任务标题、AI 入口、当前第一步、计划日期、提醒/项目、四象限和备注统一使用 16px 板块间距；短标题保持单行，长标题才按内容增高。
32. AI 拆解结果页按钮缩短为“上一步”“重拆”“采用”；采用后“当前第一步”卡片显示“撤销”，可恢复采用前的步骤。
33. 左侧独立“设置”入口移入头像右侧的账户省略号菜单；当前菜单展示设置、剩余用量和版本，未接入的用量明确标注“尚未接入”。
34. 任务详情标题栏中没有真实功能的省略号占位按钮已移除，只保留关闭按钮，避免制造不存在的任务操作。
35. 任务详情把计划日期、截止时间、提醒时间和重复方式分开；截止与提醒均精确到年月日、小时和分钟，重复支持“一次性”和“每天重复”。
36. 截止时间和提醒时间不再依赖原生日期输入框，改为自定义月历组件；支持上月、下月、跨月日期、回到今天和选中态，日期下方继续选择 00–23 小时与 00–59 分钟。
37. 月历改为按需展开：打开截止或提醒设置时先显示紧凑日期行，点击日期后才展开月历；选择日期后自动收起，避免月历和时间滚轮同时铺满详情面板。
38. 展开的月历改为覆盖小时/分钟区域的浮层，不再向下撑高整个时间组件；选中日期后浮层关闭并恢复时间选择。
39. 月历浮层由扁长比例调整为更接近正方形，增大日期格、数字、月份标题和月份切换点击区；保持覆盖显示，不改变外层组件高度。
40. 任务卡元信息中的文件夹图标单独下移 1px，与截止时间和项目文字的视觉基线对齐；不改变整行高度或其他图标位置。
41. 根据实际截图复核，将文件夹图标从前一试验位置改为相对正常位置上移 2px；时间图标、文字和整行高度继续保持不变。
42. 在“已完成”中取消完成状态后，任务恢复到原列表，但界面继续停留在“已完成”，不再强制跳转到“今天”；详情收起并通过提示说明恢复位置。
43. 账户菜单“剩余用量”卡片收紧标题、说明和上下内边距，并改为可点击的 AI 接口配置入口。
44. 新增 AI 接口设置页，可选择 DeepSeek、OpenAI 兼容或自定义供应商，并输入请求地址、模型名称和密码型 API Key；当前仅存于本次页面内存，不发起真实请求。
45. 设置页和 AI 接口页共用的标题图标改为相对整段标题文字垂直居中，关闭按钮仍固定在右上方。
46. “快捷记录”设置改为可录制组合键；点击后直接按新组合键即可更新，Esc 取消，浏览器原型的实际唤起监听同步使用新按键。
47. 快捷键进入录制但尚未收到按键时，右侧按键框保持空白，不显示“等待输入”文案。
48. 快捷键按键框改为真实可聚焦输入控件，不再用按钮模拟输入；点击后可直接录制组合键，保存后自动失焦。
49. 从“设置”列表删除重复的“AI 接口与用量”项目；AI 配置只保留在账户菜单“剩余用量”卡片入口。
50. 删除账户菜单中重复展示的“快捷记录”行；快捷键查看与修改只保留在“设置”页面。

## 本轮待体验试验

- 将右侧原有“今天／明天／以后”三段按钮收起为一行“准备哪天做”。
- 点击后展开紧凑弹层，可快捷选择今天、明天，也可通过日期输入选择未来某一天，或选择“以后再做”且不设置日期。
- 选择具体日期后自动归入“未来”，不把“未来”作为需要用户手动理解和点击的分类。
- 本项尚未作为最终交互决定；若用户体验后认为不合适，将回退本项代码、测试和文档记录，再重新设计。

## 下一轮核心动效（尚未实施）

- 优先覆盖弹窗打开/关闭、右侧详情展开/收起、任务完成/恢复/移动、日期与项目选择层、AI 拆解/重拆/采用/撤销。
- 初始节奏暂定为 120–220ms；只解释状态从哪里来、到哪里去，不以动效拖慢保存或阻止操作。
- 支持系统“减少动态效果”偏好；关闭动效后仍能完整操作。
- 现有代码只有 hover、弹窗和提示条等零散基础过渡，不能写成系统动效已经完成。
- 动效通过自动化与人工体验后，仍需用户明确确认框架，才进入第 3 步。

## 当前仍是演示，不得误写成已完成

- 没有 SQLite 或其他真实数据库；任务、项目、重命名、完成恢复和附件都不能跨刷新保存。
- 没有真正的 Windows 全局快捷键、桌面壳、系统托盘或系统通知。
- 截止时间、提醒时间、重复方式只修改内存中的演示值，不会在对应时间发通知，也不会真的生成下一次重复任务。
- 原型中的任务日期初始值仍是演示数据；计划日期、截止时间和提醒时间虽然已在界面与内存模型中分开，但尚未持久化。
- AI 拆解与反馈后重新拆解都是本地演示内容，没有调用 DeepSeek，也没有建立个人记忆。
- 图片只做浏览器预览，没有复制到应用数据目录。
- 没有安装包、备份恢复、云同步或手机端。

## 验证状态

- 浏览器回归脚本覆盖 36 项交互：主界面、账户菜单、AI 接口打开与内存保存、设置弹窗、自定义快捷键及实际唤起、详情关闭/重开、计划日期、自定义截止/提醒月历与精确分钟、项目选单、一次性/每天重复、象限取消、完成恢复、项目维护、快捷记录、AI 拆解/重拆/采用/撤销和安排明天。
- 自动化截图位于 `onestep-app/artifacts/`，共 16 张；包括主界面、收集箱、四象限、计划日期、截止月历、提醒时间、项目、重复方式、账户菜单、设置弹窗、AI 接口设置、详情收起、项目重命名、快捷记录和 AI 状态。
- 本次交接完成前重新运行 `typecheck`、`lint`、`build` 和浏览器回归；最终结果以本文件下方“本次收尾验收”及交付回复为准。

## 实测环境

- Node：`v24.18.0`。
- `npm.cmd`：`11.16.0`；直接运行 `npm` 会命中受执行策略限制的 `npm.ps1`。
- 2026-08-08 仍未找到：`rustc`、`cargo`、`cl`、`vswhere`。
- 2026-08-08 已在 `HKLM\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients` 找到 Microsoft Edge WebView2 Runtime `151.0.4129.59`。

## 下一步

1. 留在第 2 步，完成第一轮核心动效并让用户实际体验。
2. 继续处理用户指出的界面问题；计划日期单行入口仍可按体验保留或回退。
3. 用户明确说“框架可以了，进入下一步”后，冻结一版界面。
4. 进入第 3 步：设计并实现本地持久化，优先让任务、项目、完成恢复和附件跨重启保留。
5. 暂不安装 Tauri/Rust 环境，也不接 DeepSeek。

## 本次收尾验收

- 2026-08-08 `npm.cmd run typecheck`：通过。
- 2026-08-08 `npm.cmd run lint`：通过。
- 2026-08-08 `npm.cmd run build`：通过，Vite 成功生成 `dist/`。
- 2026-08-08 浏览器回归：36/36 项通过，并重新生成 16 张截图；AI 接口表单填写/保存、自定义快捷键实际唤起、截止/提醒月历与任意分钟均验证成功。
- Markdown 本地链接检查：通过，无断链。
- 文档旧状态词检查：通过，前一版的回归数量、WebView2 未检出结论与空工作区说法均已清除。
