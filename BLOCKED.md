# 受阻与待裁决清单

更新时间：2026-08-08

## Windows 桌面化阶段的环境阻碍

1. 未找到 Rust 工具链：`rustc`、`cargo`。
2. 未找到 MSVC C++ 构建工具入口：`cl`、`vswhere`；需要确认是否未安装，或只是当前 Shell 未加载开发环境。

这些属于以后进入 Windows 桌面化时的系统级前置条件，不阻塞当前界面迭代或下一步本地数据设计。未经用户确认，不安装、不修改执行策略、不改变系统配置。

2026-08-08 收尾复核时已在机器级注册表找到 Microsoft Edge WebView2 Runtime `151.0.4129.72`，因此 WebView2 不再列为当前阻碍；进入桌面化阶段时仍需重新验证实际构建可用性。

## 产品待确认

见 `docs/06-决策与待确认项.md`。这些问题不阻塞当前界面迭代；到提醒、附件、DeepSeek 或 Windows 打包阶段再逐项确认。

- “每天重复”的任务在完成后，是生成一个新的下一次任务，还是复用同一任务并重置完成状态，必须在本地数据阶段建表前确认。当前原型只保存选项，不执行重复调度。

## 下一技术阶段前的架构待裁决

- 正式目标数据库是 SQLite，但当前 `onestep-app/` 仍是纯浏览器应用，不能直接使用普通桌面 SQLite。进入第 3 步前需要在两条路径中做出明确选择：先用浏览器持久化适配器验证数据接口、以后迁移到 SQLite；或把最小 Tauri/SQLite 承载层提前，并在安装 Rust/C++ 工具前征得用户许可。当前只记录矛盾，不提前选型或安装依赖。

## 当前原型已知未对齐

- 快捷记录创建的演示任务目前会自动带上项目列表中的第一个项目；正式需求要求收集箱任务的项目可以为空。该项不阻塞继续体验，但应在界面冻结前修正并补浏览器回归，避免错误默认值进入持久化阶段。
